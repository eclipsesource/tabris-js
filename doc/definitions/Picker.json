{
  "type": "Picker",
  "description": "A widget with a drop-down list of items to choose from.",
  "include": ["Widget"],
  "properties": {
    "items": {
      "type": "string[]"
    },
    "selectionIndex": {
      "type": "number"
    },
    "selection": {
      "type": "string"
    }
  },
  "events": {
    "change:selectionIndex": {
      "parameters": ["picker", "[selectionIndex](#selectionIndex)", "options"],
      "description": "Fired when the selectionIndex property changes."
    },
    "change:selection": {
      "parameters": ["picker", "[selectionIndex](#selection)", "options"],
      "description": "Fired when the selection property changes."
    },
    "select": {
      "parameters": ["picker", "[selection](#selection)", "options"],
      "description": "Fired an item was selected by the user. The index of the selected item is given in the options object: `{index: number}`."
    }
  },
  "links": [
    {
      "title": "Simple Picker snippet",
      "path": "https://github.com/eclipsesource/tabris-js/blob/master/snippets/picker/picker.js"
    }
  ]
}
