{
  "type": "ContentView",
  "category": "widget",
  "generics": [
     {
       "name": "ChildType",
       "extends": "Widget",
       "default": "Widget"
     }
  ],
  "description": "A composite that does not require (or support) a parent to be visible. It also can not be disposed. Every instance of `ContentView` is controlled by an associated non-widget object, either an instance of `Popover` or the global `tabris` object.",
  "extends": {
    "interface": "Composite",
    "generics": ["ChildType"]
  },
  "object": "contentView",
  "constructor": {
    "access": "protected",
    "parameters": [
      {
        "name": "properties",
        "type": "Properties<ContentView>",
        "optional": true,
        "description": "Sets all key-value pairs in the properties object as widget properties."
      }
    ]
  }
}
