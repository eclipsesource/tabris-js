{
  "type": "Column",
  "description": "A composite with the `layout` property initialized with a `ColumnLayout` .",
  "extends": "Composite",
  "constructor": {
    "access": "public",
    "parameters": [
      {
        "name": "properties",
        "type": "PropertiesObject",
        "optional": true,
        "description": "Sets all key-value pairs in the properties object as widget properties."
      }
    ]
  },
  "properties": {
    "layout": {
      "type": "ColumnLayout",
      "default": "ColumnLayout",
      "const": true,
      "description": "The column layout manager responsible for interpreting the [`layoutData`](./Widget.md#layoutData) of the child widgets of this Composite."
    },
    "spacing": {
      "type": "number",
      "default": 16,
      "const": true,
      "description": "Additional space to add between the children in device independent pixel."
    }
  }
}
