{
  "type": "ColumnLayout",
  "extends": "Layout",
  "constructor": {
    "access": "public",
    "parameters": [
      {
        "name": "parameterObject",
        "description": "Sets the padding and spacing properties. If padding is set to a number, that value will be used for all four edge values.",
        "type": "{padding?: BoxDimensions|number, spacing?: number}",
        "optional": true
      }
    ]
  },
  "description": "Column based layout manager.",
  "properties": {
    "spacing": {
      "type": "number",
      "const": true,
      "default": 0,
      "description": "Additional space to add between the children in device independent pixel."
    }
  },
  "statics": {
    "properties": {
      "default": {
        "description": "Instance of ColumnLayout used as the default `layout` property value of `Column` widgets. Equivalent to `new ColumnLayout({padding: 16, spacing: 16})`",
        "type": "ColumnLayout"
      }
    }
  }
}
