{
  "type": "ConstraintLayout",
  "extends": "Layout",
  "constructor": {
    "access": "public",
    "parameters": [
      {
        "name": "parameterObject",
        "description": "Sets the padding property. If set to a number, this value will be used for all four edge values. Defaults to 0.",
        "type": "{padding?: BoxDimensions|number}",
        "optional": true
      }
    ]
  },
  "description": "The default, constraints-based layout.",
  "statics": {
    "properties": {
      "default": {
        "description": "Instance of ConstraintLayout used as the default `layout` property value of `Composite`, `Page`, `Tab` and `Canvas`. Equivalent to `new ConstraintLayout()`",
        "type": "ConstraintLayout"
      }
    }
  }
}
