{
  "type": "ConstraintLayout",
  "extends": "Layout",
  "constructor": {
    "access": "public"
  },
  "description": "The default, constraints-based layout.",
  "statics": {
    "properties": {
      "default": {
        "description": "Instance of ConstraintLayout used as the default `layout` property value of `Composite`, `Page`, `Tab` and `Canvas`. Equivalent to `new ConstraintLayout()`",
        "type": "ConstraintLayout"
      }
    }
  }
}
