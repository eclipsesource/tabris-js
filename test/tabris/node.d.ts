// Including the entire node type declarations in the tests
// can cause conflicts. Declare only the few actually used
// global variables

declare function setTimeout(fn: Function, time?: number): any;
